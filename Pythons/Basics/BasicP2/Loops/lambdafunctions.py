# we can use lambda function to perform normal task with few lines only.
# we will not required to write function to perform repeated tasks.

# use `lambda` to define lambda functions

#  Syntax: lambda ("parameters"): logic

sum = lambda a,b: a+b
print(sum(4,5))

lambda a,b: a+b
print((lambda a,b: a+b) (2,4))